# group_vars in Ansible

The `group_vars/` directory is used to define **variables for a group of hosts**.
These variables are automatically loaded by Ansible based on the inventory group name.

---

## 📂 Directory Structure
