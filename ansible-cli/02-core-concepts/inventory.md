The inventory defines the list of managed hosts.

Supports:
- Groups
- Variables
- Static (INI/YAML)
- Dynamic (cloud)

### Real-time use case
Group web servers and database servers separately to apply different configurations.
