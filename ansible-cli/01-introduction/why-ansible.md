📘 Why Ansible?

Ansible is popular because it is:

- Agentless (uses SSH / WinRM)
- Easy to learn (YAML)
- Idempotent (safe to re-run)
- Backed by a strong community

### Real-time use case
A DevOps engineer can configure 100 servers with a single playbook instead
of logging into each server manually.

